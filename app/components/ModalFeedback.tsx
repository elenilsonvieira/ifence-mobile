// Arquivo legado descontinuado. Não utilizar.
// O ModalFeedback válido encontra-se na raiz do app em `components/ModalFeedback.tsx`.
// Este arquivo é mantido apenas para evitar imports quebrados; exporta um componente no-op para não gerar warnings.
import React from 'react';
export default function __NoopModalFeedback() { return null; }